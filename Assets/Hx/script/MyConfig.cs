using UnityEngine;

public class MyConfig : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        WebCamTexture webcamTexture = new WebCamTexture();
        //print("******************webcamTexture.deviceName:" + webcamTexture.deviceName);
    }

}
